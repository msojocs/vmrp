#=========================================================================================
#========================================MMI LIB==========================================
#=========================================================================================

# multilanguage config for cstar
ifeq ($(strip $(IM_ENGINE)), CSTAR)
  ifeq ($(strip $(COMPILER)), RVCT)
    ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)), TRUE)
          3RD_LIBS           =Third-party/cstar/Arm_lib/rvct_le/csk2_full.a
        else
           ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
              3RD_LIBS           =Third-party/cstar/Arm_lib/rvct_le/csk2_full.a  
            else
              3RD_LIBS           =Third-party/cstar/Arm_lib/rvct_le/csk2_latin.a
            endif 
        endif               
    else 
        ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)), TRUE)
           3RD_LIBS           =Third-party/cstar/Arm_lib/rvct3.1/csk2_full.a
        else
            ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
              3RD_LIBS           =Third-party/cstar/Arm_lib/rvct3.1/csk2_full.a  
            else
              3RD_LIBS           =Third-party/cstar/Arm_lib/rvct3.1/csk2_latin.a
            endif 
        endif
    endif
  else
    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)), TRUE)
              3RD_LIBS           =Third-party/cstar/Arm_lib/LE/csk2_full.a
            else
               ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
                  3RD_LIBS           =Third-party/cstar/Arm_lib/LE/csk2_full.a  
                else
                  3RD_LIBS           =Third-party/cstar/Arm_lib/LE/csk2_latin.a
                endif 
            endif               
        else 
            ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)), TRUE)
               3RD_LIBS           =Third-party/cstar/Arm_lib/normal/csk2_full.a
            else
                ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
                  3RD_LIBS           =Third-party/cstar/Arm_lib/normal/csk2_full.a  
                else
                  3RD_LIBS           =Third-party/cstar/Arm_lib/normal/csk2_latin.a
                endif 
            endif
        endif
    else
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)), TRUE)
              3RD_LIBS           =Third-party/cstar/Arm_lib/nostack_le/csk2_full.a
            else
               ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
                 3RD_LIBS           =Third-party/cstar/Arm_lib/nostack_le/csk2_full.a  
               else
                 3RD_LIBS           =Third-party/cstar/Arm_lib/nostack_le/csk2_latin.a
               endif 
            endif               
        else 
            ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)), TRUE)
              3RD_LIBS           =Third-party/cstar/Arm_lib/nostack/csk2_full.a
            else
                ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
                  3RD_LIBS           =Third-party/cstar/Arm_lib/nostack/csk2_full.a 
                else
                  3RD_LIBS           =Third-party/cstar/Arm_lib/nostack/csk2_latin.a
                endif 
            endif
        endif
    endif
  endif
endif
# end ime for cstar

# IA JAVA lib config
ifeq ($(strip $(JAVA_SUPPORT)), IA)
   IA_JAVA_PATH:=
   ifeq ($(strip $(SPRD_CUSTOMER)), FALSE)
      IA_JAVA_PATH:=Third-party/formal/ia_java/Arm_lib
   else
      IA_JAVA_PATH:=Third-party/ia_java/Arm_lib
   endif
   
   ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
    IA_JAVA_PATH:=$(strip $(IA_JAVA_PATH))/le
   endif
   
   ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
      IA_JAVA_PATH:=$(strip $(IA_JAVA_PATH))/normal
   else
      IA_JAVA_PATH:=$(strip $(IA_JAVA_PATH))/nostack
   endif
   
   ifeq ($(strip $(SRAM_SIZE)), 32MBIT)
         IA_JAVA_PATH:=$(strip $(IA_JAVA_PATH))/reduce
   endif

   3RD_LIBS   += $(strip $(IA_JAVA_PATH))/ajsc_arm_rvct.a \
    $(strip $(IA_JAVA_PATH))/libmjb2.a \
    $(strip $(IA_JAVA_PATH))/libaicrypto.a
endif

#MYRIAD JAVA LIB CONFIG
ifeq ($(strip $(JAVA_SUPPORT)), MYRIAD)
    MYRIAD_JAVA_PATH:=

    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
    MYRIAD_JAVA_PATH:= Third-party/myriad_java/Arm_lib/normal
    else
    MYRIAD_JAVA_PATH:= Third-party/myriad_java/Arm_lib/nostack
    endif
    
    ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)    
    MYRIAD_JAVA_PATH:=$(strip $(MYRIAD_JAVA_PATH))/le
    endif

		ifeq ($(strip $(JAVA_SUPPORT_TYPE)), SVM)    
    MYRIAD_JAVA_PATH:=$(strip $(MYRIAD_JAVA_PATH))/svm
    endif
    
    3RD_LIBS   += $(strip $(MYRIAD_JAVA_PATH))/libjbedfbcc.a
    
endif

#SUN JAVA lib config
ifeq ($(strip $(JAVA_SUPPORT)), SUN)
    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
      ifeq ($(strip $(PLATFORM)),SC8800H)
              ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/normal_le/jvm.a
              else
                3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/normal/jvm.a
              endif
      else
         ifeq ($(strip $(PLATFORM)),SC6800H)
      ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/le/jvm.a
      else
        3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/normal/jvm.a
      endif
         else
             ifeq ($(strip $(PLATFORM)),SC8800G)
                ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                  3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/normal_le/jvm.a
                else
                  3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/normal/jvm.a
                endif
             else
                3RD_LIBS      +=Third-party/Sun_Java/arm/arm7/jvm.a
             endif
         endif
      endif
    else
        ifeq ($(strip $(PLATFORM)),SC8800H)
            3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/nostack/jvm.a
        else
              ifeq ($(strip $(PLATFORM)),SC6800H)
                    3RD_LIBS     +=Third-party/Sun_Java/arm/arm9/nostack/jvm.a
              else
                ifeq ($(strip $(PLATFORM)),SC8800G)
                    ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                      3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/nostack_le/jvm.a
                    else
                      3RD_LIBS      +=Third-party/Sun_Java/arm/arm9/nostack/jvm.a
                    endif
                else
                 3RD_LIBS     +=Third-party/Sun_Java/arm/arm7/jvm.a
                endif
            endif
        endif
    endif  
endif 

ifeq ($(strip $(MET_MEX_SUPPORT)), TRUE)
	ifeq ($(strip $(STACK_CHECK_SUPPORT)),TRUE)
		3RD_LIBS        +=  Third-party/met/lib/stack/Met_Mex.a
	else
		3RD_LIBS        +=  Third-party/met/lib/nostack/Met_Mex.a
	endif
endif         

# multilanguage config for SOGOU
ifeq ($(strip $(IM_ENGINE)), SOGOU)

    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            SOGOU_LOCALPATH = Third-party/sogou/Arm_lib/normal_li
        else
            SOGOU_LOCALPATH = Third-party/sogou/Arm_lib/normal
        endif
    else
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            SOGOU_LOCALPATH = Third-party/sogou/Arm_lib/nostack_li
        else
            SOGOU_LOCALPATH = Third-party/sogou/Arm_lib/nostack
        endif
    endif

    ifeq ($(strip $(COMPILER)), RVCT)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            SOGOU_LOCALPATH = Third-party/sogou/Arm_lib/rvct_li
        else
            SOGOU_LOCALPATH = Third-party/sogou/Arm_lib/rvct
        endif
    endif

    ifneq ($(strip $(TOUCHPANEL_TYPE)), NONE)
        SOGOU_IME_PATH = $(strip $(SOGOU_LOCALPATH))/IME_PDA
    else
        ifeq ($(strip $(KEYPAD_TYPE)), QWERTY_KEYPAD)
            SOGOU_IME_PATH = $(strip $(SOGOU_LOCALPATH))/IME_QWERTY
        endif
        ifeq ($(strip $(KEYPAD_TYPE)), TYPICAL_KEYPAD)
            SOGOU_IME_PATH = $(strip $(SOGOU_LOCALPATH))/IME_TYPICAL
        endif
    endif

    ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
        SOGOU_HW_PATH = $(strip $(SOGOU_LOCALPATH))/HW
    endif
    
    ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)), TRUE)
        SOGOU_NETWORK_PATH = $(strip $(SOGOU_LOCALPATH))/Network/Sogou_IME_NET.a
        3RD_LIBS += $(strip $(SOGOU_NETWORK_PATH)) 
    endif

    ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
        ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)),TRUE)
            3RD_LIBS += $(strip $(SOGOU_HW_PATH))/Aiwrite.a
        else
            ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)),TRUE)
                3RD_LIBS += $(strip $(SOGOU_HW_PATH))/Aiwrite.a
            endif
        endif
        3RD_LIBS += $(strip $(SOGOU_HW_PATH))/Sogou_HW_English.a \
                    $(strip $(SOGOU_HW_PATH))/Sogou_HW_Number.a \
                    $(strip $(SOGOU_HW_PATH))/Sogou_HWCode.a \
                    $(strip $(SOGOU_HW_PATH))/Sogou_Public.a 
    endif

    3RD_LIBS += $(strip $(SOGOU_IME_PATH))/Sogou_IME_English.a

    ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)),TRUE)
        ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)),TRUE)
            ifeq ($(strip $(LOW_MEMORY_SUPPORT)),NONE)
              3RD_LIBS += $(strip $(SOGOU_IME_PATH))/Sogou_IMECode_CN.a \
                        $(strip $(SOGOU_IME_PATH))/Sogou_IME_CNS.a \
                        $(strip $(SOGOU_IME_PATH))/Sogou_IME_CNT.a
            else
              3RD_LIBS += $(strip $(SOGOU_IME_PATH))/Sogou_IMECode_CN_8K.a \
                        $(strip $(SOGOU_IME_PATH))/Sogou_IME_CNS_8K.a \
                        $(strip $(SOGOU_IME_PATH))/Sogou_IME_CNT.a
            endif            
        else
            ifeq ($(strip $(LOW_MEMORY_SUPPORT)),NONE)
              3RD_LIBS += $(strip $(SOGOU_IME_PATH))/Sogou_IMECode_CNS.a \
                        $(strip $(SOGOU_IME_PATH))/Sogou_IME_CNS.a 
            else
              3RD_LIBS += $(strip $(SOGOU_IME_PATH))/Sogou_IMECode_CNS_8K.a \
                        $(strip $(SOGOU_IME_PATH))/Sogou_IME_CNS_8K.a 
            endif
        endif
    else
        ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)),TRUE)
            3RD_LIBS += $(strip $(SOGOU_IME_PATH))/Sogou_IMECode_CNT.a \
                        $(strip $(SOGOU_IME_PATH))/Sogou_IME_CNT.a 
        else
            3RD_LIBS += $(strip $(SOGOU_IME_PATH))/Sogou_IMECode_FO.a 
        endif
    endif

    ifeq ($(strip $(IM_ARABIC_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Arabic.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Arabic.a
        endif
    endif

    ifeq ($(strip $(IM_THAI_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Thai.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Thai.a
        endif
    endif

    ifeq ($(strip $(IM_VIETNAMESE_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Vietnamese.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Vietnamese.a
        endif
    endif

    ifeq ($(strip $(IM_PORTUGUESE_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Portuguese.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Portuguese.a
        endif
    endif

    ifeq ($(strip $(IM_RUSSIAN_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Russian.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Russian.a
        endif
    endif

    ifeq ($(strip $(IM_HINDI_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Hindi.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Hindi.a
        endif
    endif

    ifeq ($(strip $(IM_INDONESIAN_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Indonesian.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Indonesian.a
        endif
    endif

    ifeq ($(strip $(IM_GREEK_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Greek.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Greek.a
        endif
    endif

    ifeq ($(strip $(IM_PERSIAN_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Persian.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Persian.a
        endif
    endif

    ifeq ($(strip $(IM_MALAY_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Malay.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Malay.a
        endif
    endif

    ifeq ($(strip $(IM_FRENCH_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_French.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_French.a
        endif
    endif

    ifeq ($(strip $(IM_TAGALOG_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Tagalog.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Tagalog.a
        endif
    endif

    ifeq ($(strip $(IM_URDU_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Urdu.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Urdu.a
        endif
    endif

    ifeq ($(strip $(IM_ITALIAN_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Italian.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Italian.a
        endif
    endif

    ifeq ($(strip $(IM_SPANISH_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Spanish.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Spanish.a
        endif
    endif

    ifeq ($(strip $(IM_GERMAN_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_German.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_German.a
        endif
    endif

    ifeq ($(strip $(IM_BENGALI_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Bengali.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Bengali.a
        endif
    endif

    ifeq ($(strip $(IM_TELUGU_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Telugu.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Telugu.a
        endif
    endif

    ifeq ($(strip $(IM_MARATHI_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Marathi.a
        # Sogou doesn't support hand writing function of this language
    endif

    ifeq ($(strip $(IM_TAMIL_SUPPORT)),TRUE)
        3RD_LIBS    +=  $(strip $(SOGOU_IME_PATH))/Sogou_IME_Tamil.a
        ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
            3RD_LIBS    +=  $(strip $(SOGOU_HW_PATH))/Sogou_HW_Tamil.a
        endif
    endif


	ifeq ($(strip $(IM_MYANMAR_SUPPORT)),TRUE)
	    3RD_LIBS   	+=	$(strip $(SOGOU_IME_PATH))/Sogou_IME_Burmese.a
		ifeq ($(strip $(IM_HANDWRITING)),SOGOU)
	    	3RD_LIBS   	+=	$(strip $(SOGOU_HW_PATH))/Sogou_HW_Burmese.a
		endif
	endif
	
	ifeq ($(strip $(IM_SWAHILI_SUPPORT)),TRUE)
	    3RD_LIBS   	+=	$(strip $(SOGOU_IME_PATH))/Sogou_IME_Swahili.a
	endif
	
	ifeq ($(strip $(IM_PUNJABI_SUPPORT)),TRUE)
	    3RD_LIBS   	+=	$(strip $(SOGOU_IME_PATH))/Sogou_IME_Punjabi.a
	endif
	
	ifeq ($(strip $(IM_KHMER_SUPPORT)),TRUE)
	    3RD_LIBS   	+=	$(strip $(SOGOU_IME_PATH))/Sogou_IME_Khmer.a
	endif
	
	ifeq ($(strip $(IM_HAUSA_SUPPORT)),TRUE)
	    3RD_LIBS   	+=	$(strip $(SOGOU_IME_PATH))/Sogou_IME_Hausa.a
	endif
	
	ifeq ($(strip $(IM_GUJARATI_SUPPORT)),TRUE)
	    3RD_LIBS   	+=	$(strip $(SOGOU_IME_PATH))/Sogou_IME_Gujarati.a
	endif
	
	ifeq ($(strip $(IM_LAO_SUPPORT)),TRUE)
	    3RD_LIBS   	+=	$(strip $(SOGOU_IME_PATH))/Sogou_IME_Lao.a
	endif
endif
# end ime for SOGOU 
#Handwriting
ifeq ($(strip $(SPRD_CUSTOMER)), FALSE)
#    ifeq ($(strip $(PLATFORM)), SC8800H)
#        HW_FORMAL_PATH =formal/hw
#    else
    HW_FORMAL_PATH =hw
#    endif
else
    HW_FORMAL_PATH =hw
endif

ifeq ($(strip $(IM_HANDWRITING)), HANWANG)
    ifeq ($(strip $(COMPILER)), RVCT)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/Europhw.a
            	ifeq ($(strip $(IM_SIMP_CHINESE_SUPPORT)), TRUE)
	              ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
	                  3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/ChshwJF.a
	              else               
	                  3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/Chshw.a
	              endif
	            else
	            	ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
	                  3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/Chshw_t_min.a
	              endif
	            endif
            ifeq ($(strip $(IM_ARABIC_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/ArabicHW.a
            endif
            ifeq ($(strip $(IM_VIETNAMESE_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/VietHW.a
            endif
            ifeq ($(strip $(IM_THAI_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/ThaiHW.a
            endif
            ifeq ($(strip $(IM_PERSIAN_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/PersianHW.a
            endif
            ifeq ($(strip $(IM_URDU_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/PersianHW.a
            endif
            ifeq ($(strip $(IM_HEBREW_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/HebrewHW.a
            endif
            ifeq ($(strip $(IM_HINDI_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/Hindihw.a
            endif
            ifeq ($(strip $(IM_BENGALI_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/rvct_le/BengaliHw.a
            endif    
        else
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/Europhw.a
            ifeq ($(strip $(LOW_MEMORY_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/Chshw.a
            else
                ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/Chshw_t_min.a
                else               
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/Chshw.a
                endif            
            endif
            ifeq ($(strip $(IM_ARABIC_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/ArabicHW.a
            endif
            ifeq ($(strip $(IM_VIETNAMESE_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/VietHW.a
            endif
            ifeq ($(strip $(IM_THAI_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/ThaiHW.a
            endif
            ifeq ($(strip $(IM_PERSIAN_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/PersianHW.a
            endif
            ifeq ($(strip $(IM_URDU_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/PersianHW.a
            endif
            ifeq ($(strip $(IM_HEBREW_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/HebrewHW.a
            endif
            ifeq ($(strip $(IM_HINDI_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/Hindihw.a
            endif
            ifeq ($(strip $(IM_BENGALI_SUPPORT)), TRUE)
            3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/RVCT3.1/BengaliHw.a
            endif
        endif
    else
        ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
            ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/chshw.a
                ifeq ($(strip $(IM_ARABIC_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Arabichw.a
                endif
                ifeq ($(strip $(IM_VIETNAMESE_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Viethw.a
                endif
                ifeq ($(strip $(IM_PORTUGUESE_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_RUSSIAN_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_GREEK_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_FRENCH_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_HUNGARIAN_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_ITALIAN_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_TURKISH_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_SPANISH_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_GERMAN_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_ROMANIAN_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_SLOVENIAN_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_CZECH_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_MALAY_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_TAGALOG_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_INDONESIAN_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Europhw.a
                endif
                ifeq ($(strip $(IM_THAI_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/Thaihw.a
                endif
                ifeq ($(strip $(IM_PERSIAN_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/PersianHW.a
                endif
                ifeq ($(strip $(IM_URDU_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/PersianHW.a
                endif
                ifeq ($(strip $(IM_HEBREW_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/HebrewHW.a
                endif
                ifeq ($(strip $(IM_HINDI_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/HindiHW.a
                endif
                ifeq ($(strip $(IM_BENGALI_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/LE_S/BengaliHW.a
                endif
            else
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                ifeq ($(strip $(LOW_MEMORY_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/s_chinese/normal/chshw.a
                else
                    ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
                        3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/st_chinese/chshw.a
                    else               
                        3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/s_chinese/enhance/chshw.a
                    endif              
                endif
                ifeq ($(strip $(IM_ARABIC_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Arabic/Arabichw.a
                endif
                ifeq ($(strip $(IM_VIETNAMESE_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Viet/Viethw.a
                endif
                ifeq ($(strip $(IM_PORTUGUESE_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_RUSSIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_GREEK_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_FRENCH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_HUNGARIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_ITALIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_TURKISH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_SPANISH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_GERMAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_ROMANIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_SLOVENIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_CZECH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_MALAY_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_TAGALOG_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_INDONESIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_THAI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Thai/Thaihw.a
                endif
                ifeq ($(strip $(IM_PERSIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Persian/PersianHW.a
                endif
                ifeq ($(strip $(IM_URDU_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Persian/PersianHW.a
                endif
                ifeq ($(strip $(IM_HEBREW_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Hebrew/HebrewHW.a
                endif
                ifeq ($(strip $(IM_HINDI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Hindi/HindiHW.a
                endif
                ifeq ($(strip $(IM_BENGALI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/normal/Bengali/BengaliHW.a
                endif
            endif
        else
            ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                ifeq ($(strip $(LOW_MEMORY_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/s_chinese/min/chshw.a
                else
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/s_chinese/min/chshw.a
                    endif
                ifeq ($(strip $(IM_ARABIC_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Arabic/Arabichw.a
                endif
                ifeq ($(strip $(IM_VIETNAMESE_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Viet/Viethw.a
                endif
                ifeq ($(strip $(IM_PORTUGUESE_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_RUSSIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_GREEK_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_FRENCH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_HUNGARIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_ITALIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_TURKISH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_SPANISH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_GERMAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_ROMANIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_SLOVENIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_CZECH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_MALAY_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_TAGALOG_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_INDONESIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_THAI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Thai/Thaihw.a
                endif
                ifeq ($(strip $(IM_PERSIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Persian/PersianHW.a
                endif
                ifeq ($(strip $(IM_URDU_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Persian/PersianHW.a
                endif
                ifeq ($(strip $(IM_HEBREW_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Hebrew/HebrewHW.a
                endif
                ifeq ($(strip $(IM_HINDI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Hindi/HindiHW.a
                endif
                ifeq ($(strip $(IM_BENGALI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack_le/Bengali/BengaliHW.a
                endif    
            else
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                ifeq ($(strip $(LOW_MEMORY_SUPPORT)), TRUE)
                    3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/s_chinese/normal/chshw.a
                else
                    ifeq ($(strip $(IM_TRAD_CHINESE_SUPPORT)), TRUE)
                        3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/st_chinese/chshw.a
                    else               
                        3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/s_chinese/normal/chshw.a
                    endif            
                endif
                ifeq ($(strip $(IM_ARABIC_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Arabic/Arabichw.a
                endif
                ifeq ($(strip $(IM_VIETNAMESE_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Viet/Viethw.a
                endif
                ifeq ($(strip $(IM_PORTUGUESE_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_RUSSIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_GREEK_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_FRENCH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_HUNGARIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_ITALIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_TURKISH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_SPANISH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_GERMAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_ROMANIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_SLOVENIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_CZECH_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_MALAY_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_TAGALOG_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_INDONESIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Europ/Europhw.a
                endif
                ifeq ($(strip $(IM_THAI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Thai/Thaihw.a
                endif
                ifeq ($(strip $(IM_PERSIAN_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Persian/PersianHW.a
                endif
                ifeq ($(strip $(IM_URDU_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Persian/PersianHW.a
                endif
                ifeq ($(strip $(IM_HEBREW_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Hebrew/HebrewHW.a
                endif
                ifeq ($(strip $(IM_HINDI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Hindi/HindiHW.a
                endif
                ifeq ($(strip $(IM_BENGALI_SUPPORT)), TRUE)
                3RD_LIBS            +=Third-party/$(strip $(HW_FORMAL_PATH))/Arm_lib/nostack/Bengali/BengaliHW.a
                endif
            endif
        endif
    endif
endif

#nxp
ifeq ($(strip $(NXP_LVVE_SUPPORT)), TRUE)
ifeq ($(strip $(DUALMIC)), TRUE)
	ifeq ($(strip $(COMPILER)), ADS)
		3RD_LIBS  += MS_Ref/export/lib/REL_LVVE_HPF_RX_EQ_VOL_HF_NV_FENS_VC_WM_RX_WB_DRC_RX_CNG_NG_ARM9E_ADS_demo.a
	endif
else
		ifeq ($(strip $(COMPILER)), ADS)
			3RD_LIBS  += MS_Ref/export/lib/REL_LVVE_HPF_EQ_VOL_HF_FENS_VC_WM_WB_DRC_TX_CNG_ARM9E_ADS_T1D2D848SWSTFY.a
		else
			ifeq ($(strip $(RVCT_VERSION)), V41)
				3RD_LIBS  += MS_Ref/export/lib/REL_LVVE_HPF_RX_EQ_VOL_HF_FENS_VC_WM_RX_WB_DRC_TX_CNG_ARM9E_RVCT_T4D1D561.a
			else 
				3RD_LIBS  += MS_Ref/export/lib/REL_LVVE_HPF_RX_EQ_VOL_HF_FENS_VC_WM_RX_WB_DRC_TX_CNG_ARM9E_RVCT_T3D1.a
			endif
	endif
endif
endif

#mv
ifeq ($(strip $(MV_SUPPORT)), IFLYTEK)
  ifneq ($(strip $(COMPILER)), RVCT)
    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
      ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        3RD_LIBS   +=   Third-party/mv/Arm_lib/normal/ivMagicVoice_arm9_li.a
      else
        3RD_LIBS   +=   Third-party/mv/Arm_lib/normal/ivMagicVoice_arm9_bi.a
      endif
    else
      ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        3RD_LIBS   +=   Third-party/mv/Arm_lib/nostack/ivMagicVoice_arm9_li.a
      else
        3RD_LIBS   +=   Third-party/mv/Arm_lib/nostack/ivMagicVoice_arm9_bi.a
      endif
    endif
  else
      ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        3RD_LIBS   +=   Third-party/mv/Arm_lib/rvct31/ivMagicVoice_arm9_li.a
      else
        3RD_LIBS   +=   Third-party/mv/Arm_lib/rvct31/ivMagicVoice_arm9_bi.a
      endif
  endif
endif
#add for sky app start
ifeq ($(strip $(MRAPP_SUPPORT)), TRUE)
ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
3RD_LIBS			+=Third-party/SKYENGINE/Arm_lib/normal/mythroad.lib
3RD_LIBS			+=Third-party/SKYENGINE/Arm_lib/normal/skyApn.a
3RD_LIBS			+=Third-party/SKYENGINE/Arm_lib/normal/skyDove.a
else
3RD_LIBS			+=Third-party/SKYENGINE/Arm_lib/nostack/mythroad.lib
3RD_LIBS			+=Third-party/SKYENGINE/Arm_lib/nostack/skyApn.lib
3RD_LIBS			+=Third-party/SKYENGINE/Arm_lib/nostack/skyDove.a
ifeq ($(strip $(__MMI_SKYQQ__)), TRUE)
3RD_LIBS			+=MS_MMI/source/mmi_app/app/skyqq/lib/skyqq.a
endif
endif
endif
#add for sky app end

#tts
ifeq ($(strip $(TTS_SUPPORT)),IFLYTEK)
    ifeq ($(strip $(COMPILER)), RVCT)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            3RD_LIBS   +=  Third-party/InterSound4/Arm_lib/rvct_le/Aisound4.a
        else
            3RD_LIBS   +=  Third-party/InterSound4/Arm_lib/rvct_be/Aisound4.a
        endif
    else
       ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
          ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
               3RD_LIBS   +=  Third-party/InterSound4/Arm_lib/littleendian/normal/Aisound4.a
          else
              3RD_LIBS   +=  Third-party/InterSound4/Arm_lib/littleendian/nostack/Aisound4.a
          endif
       else
           ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
              3RD_LIBS   +=  Third-party/InterSound4/Arm_lib/normal/Aisound4.a
           else
              3RD_LIBS   +=  Third-party/InterSound4/Arm_lib/nostack/Aisound4.a
           endif
       endif
    endif
endif

ifeq ($(strip $(TTS_SUPPORT)),SHYOUNGTONE)
    ifeq ($(strip $(COMPILER)), RVCT)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            3RD_LIBS   +=  Third-party/youngtone/lib/rvct_le/yt_tts_english_hindi_bahasa.a
        else
            3RD_LIBS   +=  Third-party/youngtone/lib/rvct_be/yt_tts_english_hindi_bahasa.a
        endif
    else
     ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
           3RD_LIBS   +=  Third-party/youngtone/lib/littleendian/normal/yt_tts_english_hindi_bahasa_6800_stack_le.a
        else
           3RD_LIBS   +=  Third-party/youngtone/lib/littleendian/nostack/yt_tts_english_hindi_bahasa_6800_no_stack_le.a
        endif
     else
        ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
          3RD_LIBS   +=  Third-party/youngtone/lib/normal/yt_tts_english_hindi_bahasa_6800_stack_be.a
        else
          3RD_LIBS   +=  Third-party/youngtone/lib/nostack/yt_tts_english_hindi_bahasa_6800_no_stack_be.a
        endif
    endif
  endif
endif

ifeq ($(strip $(TTS_SUPPORT)),EJ)
  ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
    3RD_LIBS   +=  Third-party/ejTTS/LE_lib/ejTTS.a
  endif
endif

ifeq ($(strip $(SXH_APPLE_SUPPORT)), TRUE)
	ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
		3RD_LIBS			+= Third-party/apple/lib/normal/liboem.a
	else
		3RD_LIBS			+=  Third-party/apple/lib/nostack/liboem_noswts.a
	endif
endif

#DM lib config 
ifeq ($(strip $(DM_SUPPORT)), REDBENCH)
    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
          ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            DM_LOCALPATH := dm/Arm_lib/LE_normal
          else
            DM_LOCALPATH := dm/Arm_lib/normal
          endif
    else
          ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            DM_LOCALPATH := dm/Arm_lib/LE_nostack
          else
            DM_LOCALPATH := dm/Arm_lib/nostack
          endif
        endif
    ifeq ($(strip $(DEV_MANAGE_SUPPORT)), TRUE)
        ifeq ($(strip $(SPRD_CUSTOMER)), FALSE)  
            3RD_LIBS      +=Third-party/formal/$(strip $(DM_LOCALPATH))/vdmengine.a \
            Third-party/formal/$(strip $(DM_LOCALPATH))/vdmfumo.a \
            Third-party/formal/$(strip $(DM_LOCALPATH))/vdmscomo.a \
            Third-party/formal/$(strip $(DM_LOCALPATH))/vdmutil.a \
            Third-party/formal/$(strip $(DM_LOCALPATH))/vdmscinv.a
        else
            3RD_LIBS      +=Third-party/$(strip $(DM_LOCALPATH))/vdmengine.a \
            Third-party/$(strip $(DM_LOCALPATH))/vdmfumo.a \
            Third-party/$(strip $(DM_LOCALPATH))/vdmscomo.a \
            Third-party/$(strip $(DM_LOCALPATH))/vdmutil.a \
            Third-party/$(strip $(DM_LOCALPATH))/vdmscinv.a
        endif
    endif
endif

#Support TechSoft Email Lib
ifneq ($(strip $(MMIEMAIL_SUPPORT)), NONE)
   ifeq ($(strip $(MMIEMAIL_SUPPORT)),TRUE)
     3RD_LIBS +=Third-party/email/arm_lib/email.a
   endif
endif

#Mcre lib config
ifeq ($(strip $(MCARE_MINI_SUPPORT)),TRUE)
    ifeq ($(strip $(STACK_CHECK_SUPPORT)),TRUE)
            3RD_LIBS += Third-party/TencentMcare/Arm_lib/TencentMcare.a
    else
        ifeq ($(strip $(FLASH_SUPPORT)),TRUE)
            3RD_LIBS += Third-party/TencentMcare/Arm_lib/flash_no_stack.a
        else
            3RD_LIBS += Third-party/TencentMcare/Arm_lib/TencentMcare_no_stack.a
        endif
    endif
    ifneq ($(strip $(COMPILER)), RVCT)
        ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
            ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                    3RD_LIBS          +=  Third-party/zlib/le/zlib.a
            else
                    3RD_LIBS          +=  Third-party/zlib/Lib/zlib.a 
            endif  
        else
            3RD_LIBS          +=  Third-party/zlib/Lib/zlib_nostack.a
        endif
    endif
endif


#QQ lib config
ifneq ($(strip $(QQ_SUPPORT)), NONE)
    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
            3RD_LIBS          +=Third-party/qq/Arm_lib/normal/qq.a
    else
            3RD_LIBS          +=Third-party/qq/Arm_lib/nostack/qq.a
    endif
endif

#push mail lib config
ifeq ($(strip $(PUSH_EMAIL_SUPPORT)), TRUE)
   ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
        3RD_LIBS   +=   Third-party/push_mail/arm_lib/normal/push_mail.a
   else
        3RD_LIBS   +=   Third-party/push_mail/arm_lib/nostack/push_mail.a
   endif
endif

#mobile video lib config
ifeq ($(strip $(MOBILE_VIDEO_SUPPORT)), TRUE)
    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            3RD_LIBS      +=MS_MMI/Source/MMI_App/App/mobile_video/lib/le/cmplayer.a
        else
            3RD_LIBS      +=MS_MMI/Source/MMI_App/App/mobile_video/lib/cmplayer.a
        endif
    else
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            3RD_LIBS      +=MS_MMI/Source/MMI_App/App/mobile_video/lib/le_no_stack/cmplayer.a
        else  
            3RD_LIBS      +=MS_MMI/Source/MMI_App/App/mobile_video/lib/bi_no_stack/cmplayer.a      
        endif
    endif
endif

ifeq ($(strip $(VECTOR_FONT_SUPPORT)),ETRUMP)
    ifeq ($(strip $(COMPILER)),ADS)
        3RD_LIBS += Third-party/etrump/lib/ads12/libet_common.spreadtrum_sc6530_rvct.a
        3RD_LIBS += Third-party/etrump/lib/ads12/libetrump.spreadtrum_sc6530_rvct.a
        3RD_LIBS += Third-party/etrump/lib/ads12/libft2_mini.spreadtrum_sc6530_rvct.a
        3RD_LIBS += Third-party/etrump/lib/ads12/libottab.spreadtrum_ssc6530_rvct.a
        3RD_LIBS += Third-party/etrump/lib/ads12/libtextlayout.spreadtrum_sc6530_rvct.a
    else
        3RD_LIBS += Third-party/etrump/lib/rvct31/libet_common.spreadtrum_sc6530_rvct.a
        3RD_LIBS += Third-party/etrump/lib/rvct31/libetrump.spreadtrum_sc6530_rvct.a
        3RD_LIBS += Third-party/etrump/lib/rvct31/libft2_mini.spreadtrum_sc6530_rvct.a
        3RD_LIBS += Third-party/etrump/lib/rvct31/libottab.spreadtrum_sc6530_rvct.a
        3RD_LIBS += Third-party/etrump/lib/rvct31/libtextlayout.spreadtrum_sc6530_rvct.a
    endif
endif

ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
    ifeq ($(strip $(NETFRONT_SUPPORT)), VER42)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                NETFRONT_LOCALPATH :=netfront/Netfront4.2/arm_lib/le_stack_rel
        else
                NETFRONT_LOCALPATH :=netfront/Netfront4.2/arm_lib/be_stack_rel
        endif
    else
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                NETFRONT_LOCALPATH :=netfront/LittleEndian/arm_lib
        else
                NETFRONT_LOCALPATH :=netfront/Netfront4.0/arm_lib
        endif
    endif
else
    ifeq ($(strip $(NETFRONT_SUPPORT)), VER42)
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                NETFRONT_LOCALPATH :=netfront/Netfront4.2/arm_lib/le_nostack_rel
        else
                NETFRONT_LOCALPATH :=netfront/Netfront4.2/arm_lib/be_nostack_rel
        endif
    else
        ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
                NETFRONT_LOCALPATH :=netfront/LittleEndian/arm_lib/nostack
        else
                NETFRONT_LOCALPATH :=netfront/Netfront4.0/arm_lib/nostack
        endif
    endif
endif

ifeq ($(strip $(BROWSER_SUPPORT_NETFRONT)), TRUE)
    ifeq ($(strip $(NETFRONT_SUPPORT)), VER42)
        ifeq ($(strip $(SPRD_CUSTOMER)), FALSE)   
            3RD_LIBS      += Third-party/formal/$(strip $(NETFRONT_LOCALPATH))/nmcs_libs.a \
                 Third-party/formal/$(strip $(NETFRONT_LOCALPATH))/nmcs_slim.a \
                 Third-party/formal/$(strip $(NETFRONT_LOCALPATH))/nmcs_nf4libs.a \
                 Third-party/formal/$(strip $(NETFRONT_LOCALPATH))/nmcs_siipeerglue.a
        else
            3RD_LIBS      += Third-party/$(strip $(NETFRONT_LOCALPATH))/nmcs_libs.a \
                         Third-party/$(strip $(NETFRONT_LOCALPATH))/nmcs_slim.a \
                         Third-party/$(strip $(NETFRONT_LOCALPATH))/nmcs_nf4libs.a \
                         Third-party/$(strip $(NETFRONT_LOCALPATH))/nmcs_siipeerglue.a
        endif
    else
        ifeq ($(strip $(SPRD_CUSTOMER)), FALSE)   
            3RD_LIBS      += Third-party/formal/$(strip $(NETFRONT_LOCALPATH))/nmcs_libs.a \
                 Third-party/formal/$(strip $(NETFRONT_LOCALPATH))/nmcs_slim.a \
                 Third-party/formal/$(strip $(NETFRONT_LOCALPATH))/nmcs_nf4libs.a \
                 Third-party/formal/$(strip $(NETFRONT_LOCALPATH))/nmcs_siipeerglue.a
        else
            3RD_LIBS      += Third-party/$(strip $(NETFRONT_LOCALPATH))/nmcs_libs.a \
                         Third-party/$(strip $(NETFRONT_LOCALPATH))/nmcs_slim.a \
                         Third-party/$(strip $(NETFRONT_LOCALPATH))/nmcs_nf4libs.a \
                         Third-party/$(strip $(NETFRONT_LOCALPATH))/nmcs_siipeerglue.a
        endif

    endif
endif

#==========================================================================================
#=======================================Ref LIB============================================
#==========================================================================================
ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
    ifeq ($(strip $(MOTION_SENSOR_TYPE)), YANGXUN_MEYE)
    3RD_LIBS      +=Third-party/met/lib/MET_VM.lib
    endif 
else
endif 

ifneq ($(strip $(COMPILER)), RVCT)
    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
        ifeq ($(strip $(CMMB_SUPPORT)), TRUE)
            ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            3RD_LIBS          +=  Third-party/zlib/le/zlib.a
            else
            3RD_LIBS          +=  Third-party/zlib/Lib/zlib.a 
            endif   
        endif
    else
        ifeq ($(strip $(CMMB_SUPPORT)), TRUE)
        3RD_LIBS          +=  Third-party/zlib/Lib/zlib_nostack.a
        
        endif
    endif
endif

ifeq ($(strip $(SPRD_CUSTOMER)), FALSE)
    BLUETOOTH_FORMAL:=formal/
else
    BLUETOOTH_FORMAL:=
endif

ifneq ($(strip $(BLUETOOTH_SUPPORT)),NONE)

    BT_LOG      =
    BT_LOW_MEM  =
    
    ifeq ($(strip $(BLUETOOTH_SUPPORT)), CSR_BC4_6)
        BT_VENDER   = bt_csr
        BT_LIB_ROOT = Third-party/$(BLUETOOTH_FORMAL)$(BT_VENDER)/bt_csr_15_3/Arm_lib
    endif
    
    ifeq ($(strip $(BLUETOOTH_SUPPORT)), CSR_BC4_6_LOW_MEM)
        BT_VENDER   = bt_csr
        BT_LIB_ROOT = Third-party/$(BLUETOOTH_FORMAL)$(BT_VENDER)/bt_csr_15_3/Arm_lib
        BT_LOW_MEM  = _low_mem_r111
    endif
       
    
    ifeq ($(strip $(BLUETOOTH_SUPPORT)), IS1662)
        BT_VENDER   = bt_issc
        BT_LIB_ROOT = Third-party/$(BLUETOOTH_FORMAL)$(BT_VENDER)/lib
        
	    ifeq ($(strip $(TRACE_INFO_SUPPORT)), TRUE)
	        BT_LOG  = _log
	    endif        
    endif       

    ifeq ($(strip $(BLUETOOTH_SUPPORT)), RTL8723)
        BT_VENDER   = bt_rtk
        BT_LIB_ROOT = Third-party/$(BLUETOOTH_FORMAL)$(BT_VENDER)/lib
        
	    ifeq ($(strip $(TRACE_INFO_SUPPORT)), TRUE)
	        BT_LOG  = _log
	    endif        
    endif       
    
    ifeq ($(strip $(COMPILER)), RVCT)
             BT_SUB_DIR = rvct
    else 
        ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
             BT_SUB_DIR = normal
        else 
             BT_SUB_DIR = nostack
        endif
    endif

    BT_ARM = arm9

    ifeq ($(strip $(RVCT_VERSION)), RVCT31)
        BT_RVCT_VER = _rvct31
    else
        BT_RVCT_VER = 
    endif   
    
    ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        BT_ENDIAN = _le
    else
        BT_ENDIAN =
    endif    
# example 
# .../nostack/bt_csr_arm7_low_mem_r111.a
# .../normal/bt_csr_arm9_le.a
# .../rvct/bt_csr_arm9_rvct31_le.a

    3RD_LIBS += $(BT_LIB_ROOT)/$(BT_SUB_DIR)/$(BT_VENDER)_$(BT_ARM)$(BT_LOW_MEM)$(BT_RVCT_VER)$(BT_ENDIAN)$(BT_LOG).a
    
endif

ifeq ($(strip $(BLUETOOTH_SUPPORT)), NONE)
   ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        3RD_LIBS          +=
   else 
       ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
            3RD_LIBS          += Third-party/bt_csr/bt_pal_csr_dummy.a
       else
            3RD_LIBS          += Third-party/bt_csr/bt_pal_csr_nostackcheck_dummy.a
       endif
    endif
endif

ifeq ($(strip $(VT_SUPPORT)), TRUE)
    ifeq ($(strip $(SPRD_CUSTOMER)), FALSE)    
        3RD_LIBS      += build/$(PROJECT)_builddir/lib/mplapi_intg_8801h.a \
            build/$(PROJECT)_builddir/lib/mplapi_threadx_8801h.a
    else
        3RD_LIBS      += Third-party/Mplapi/Arm_lib/normal/mplapi_intg_8801h.a \
            Third-party/Mplapi/Arm_lib/normal/mplapi_threadx_8801h.a
    endif
endif
#==========================================================================================
#=======================================OTHER LIB==========================================
#==========================================================================================
ifeq ($(strip $(WIFI_SUPPORT)),CSR6030)
    CSR_LOG = TRUE
    #root path
    CSR6030_WIFI_LIB_ROOT  =  Third-party/wifi/CSR6030

    #chip type
    ifeq ($(strip $(PLATFORM)), SC6600L)
        CSR_ARM_DESC = arm7
    else
        CSR_ARM_DESC = arm9
    endif
    #chip endian
    ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        CSR_ENDIAN_DESC = _le
    endif
    #log switch
    ifeq ($(strip $(CSR_LOG)),TRUE)
        CSR_LOG_DESC= -log
    endif
    #stack check
    ifeq ($(strip $(STACK_CHECK_SUPPORT)),TRUE)
        CSR_STK_DESC = -stack
    else
        CSR_STK_DESC = -nostack
    endif

    #compiler
    ifeq ($(strip $(COMPILER)), RVCT)
        CSR_COMPILER_DESC = _rvct31
        endif
    3RD_LIBS  +=  $(CSR6030_WIFI_LIB_ROOT)/frw_csr/lib/csr_frw_${CSR_ARM_DESC}${CSR_STK_DESC}${CSR_COMPILER_DESC}${CSR_ENDIAN_DESC}.a \
                  $(CSR6030_WIFI_LIB_ROOT)/lib/csr_wifi_${CSR_ARM_DESC}${CSR_STK_DESC}${CSR_COMPILER_DESC}${CSR_ENDIAN_DESC}.a
    ifeq ($(strip $(CSR_LOG)), TRUE)
      3RD_LIBS += $(CSR6030_WIFI_LIB_ROOT)/frw_csr/lib/csr_frw_spt_${CSR_ARM_DESC}${CSR_STK_DESC}${CSR_COMPILER_DESC}${CSR_ENDIAN_DESC}.a \
                  $(CSR6030_WIFI_LIB_ROOT)/frw_csr/lib/csr_frw_log_${CSR_ARM_DESC}${CSR_STK_DESC}${CSR_COMPILER_DESC}${CSR_ENDIAN_DESC}.a
    endif
endif
  
ifeq ($(findstring CSR6027,$(WIFI_SUPPORT)), CSR6027)
    CSR_LOG = FALSE
    #root path
    CSR6027_WIFI_LIB_ROOT  = Third-party/wifi/CSR6027
    #chip type
    ifeq ($(strip $(PLATFORM)), SC6600L)
        CSR_ARM_DESC = arm7
    else
        CSR_ARM_DESC = arm9
    endif

    #chip endian
    ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
        CSR_ENDIAN_DESC = _le
    endif

    #log switch
    ifeq ($(strip $(CSR_LOG)),TRUE)
        CSR_LOG_DESC= -log
    endif

    #stack check
    ifeq ($(strip $(STACK_CHECK_SUPPORT)),TRUE)
        CSR_STK_DESC = -stack
    else
        CSR_STK_DESC = -nostack
    endif

    #compiler
    ifeq ($(strip $(COMPILER)), RVCT)
        CSR_COMPILER_DESC = _rvct31
    endif

    3RD_LIBS  +=  $(CSR6027_WIFI_LIB_ROOT)/frw_csr/lib/csr_frw_${CSR_ARM_DESC}${CSR_STK_DESC}${CSR_COMPILER_DESC}${CSR_ENDIAN_DESC}.a \
                  $(CSR6027_WIFI_LIB_ROOT)/lib/csr_wifi_${CSR_ARM_DESC}${CSR_STK_DESC}${CSR_COMPILER_DESC}${CSR_ENDIAN_DESC}.a

    ifeq ($(strip $(CSR_LOG)), TRUE)
      3RD_LIBS += $(CSR6027_WIFI_LIB_ROOT)/frw_csr/lib/csr_frw_spt_${CSR_ARM_DESC}${CSR_STK_DESC}${CSR_COMPILER_DESC}${CSR_ENDIAN_DESC}.a \
                  $(CSR6027_WIFI_LIB_ROOT)/frw_csr/lib/csr_frw_log_${CSR_ARM_DESC}${CSR_STK_DESC}${CSR_COMPILER_DESC}${CSR_ENDIAN_DESC}.a
    endif
endif

ifeq ($(strip $(RELEASE_LEVEL_D0)), TRUE)
3RD_LIBS         += lib/$(strip $(PRODUCT_BASELIB_DIR))/release_level_d0.a
endif 

ifeq ($(strip $(OPERA_MINI_SUPPORT)), VER42)
    ifeq ($(strip $(PLATFORM)),SC6600L)
        ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
                3RD_LIBS += Third-party/opera/mini4/arm_lib/arm7_be_swst/opera.lib
        else
                3RD_LIBS += Third-party/opera/mini4/arm_lib/arm7_be_noswst/opera.lib
        endif
    else ifeq ($(strip $(PLATFORM)),SC6530)
        ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
                ifeq ($(strip $(COMPILER)), RVCT)
                    3RD_LIBS += Third-party/opera/mini4/arm_lib/arm9_rvct_le_swst/opera.lib
    else
                    3RD_LIBS += Third-party/opera/mini4/arm_lib/arm9_le_swst/opera.lib
                endif
        else
                ifeq ($(strip $(COMPILER)), RVCT)
                    3RD_LIBS += Third-party/opera/mini4/arm_lib/arm9_rvct_le_noswst/opera.lib
                else
                  3RD_LIBS += Third-party/opera/mini4/arm_lib/arm9_le_noswst/opera.lib
              endif
        endif
    else ifeq ($(strip $(PLATFORM)),SC6800H)
            ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
                    3RD_LIBS += Third-party/opera/mini4/arm_lib/arm9_be_swst/opera.lib
            else
                    3RD_LIBS += Third-party/opera/mini4/arm_lib/arm9_be_noswst/opera.lib
        endif
    endif
else
        ifeq ($(strip $(OPERA_MINI_SUPPORT)), VER6)
        ifeq ($(strip $(PLATFORM)),SC6800H)
            ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
                    3RD_LIBS += Third-party/opera/mini6/arm_lib/arm9_be_swst/opera.lib
            else
                    3RD_LIBS += Third-party/opera/mini6/arm_lib/arm9_be_noswst/opera.lib
            endif
        else ifeq ($(strip $(PLATFORM)),SC6530)
            ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
                    ifeq ($(strip $(COMPILER)), RVCT)
                        3RD_LIBS += Third-party/opera/mini6/arm_lib/arm9_rvct_le_swst/opera.lib
                    else
                        3RD_LIBS += Third-party/opera/mini6/arm_lib/arm9_le_swst/opera.lib
                    endif
            else
                            ifeq ($(strip $(COMPILER)), RVCT)
                                3RD_LIBS += Third-party/opera/mini6/arm_lib/arm9_rvct_le_noswst/opera.lib
                            else
                        3RD_LIBS += Third-party/opera/mini6/arm_lib/arm9_le_noswst/opera.lib
                    endif
            endif
        endif
    endif
endif

ifeq ($(strip $(OPERA_MOBILE_SUPPORT)), V11)
    ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
            3RD_LIBS += Third-party/opera/mobile11/arm_lib/arm9_le_swst/opera1.lib Third-party/opera/mobile11/arm_lib/arm9_le_swst/opera2.lib
    else
            3RD_LIBS += Third-party/opera/mobile11/arm_lib/arm9_le_noswst/opera1.lib Third-party/opera/mobile11/arm_lib/arm9_le_noswst/opera2.lib
    endif
endif

ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
    ifeq ($(strip $(EXSTERN_WIFI_CO_EXIST)), TRUE)
             3RD_LIBS     +=Third-party/wifi_atheros/athr_btfilter.a
    endif         
else
    ifeq ($(strip $(EXSTERN_WIFI_CO_EXIST)), TRUE)
             3RD_LIBS     +=Third-party/wifi_atheros/athr_btfilter_no_stack.a
    endif         
endif

ifneq ($(strip $(COMPILER)), RVCT)
    ifeq ($(strip $(WRE_SUPPORT)),TRUE)
        ifeq ($(strip $(STACK_CHECK_SUPPORT)), TRUE)
            ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            3RD_LIBS          +=  Third-party/zlib/le/zlib.a
            else
            3RD_LIBS          +=  Third-party/zlib/Lib/zlib.a 
            endif  
        else
            ifeq ($(strip $(CHIP_ENDIAN)), LITTLE)
            3RD_LIBS          +=  Third-party/zlib/le/zlib_nostack.a
            else
            3RD_LIBS          +=  Third-party/zlib/Lib/zlib_nostack.a
                endif
        endif
    endif
endif

#mcare V31 lib
ifeq ($(strip $(MCARE_V31_SUPPORT)),TRUE)

		ifeq ($(strip $(FLASH_SUPPORT)),TRUE)
			3RD_LIBS += Third-party/TencentMcare/Arm_lib/flash_no_stack.a
		else
			ifeq ($(strip $(STACK_CHECK_SUPPORT)),TRUE)
			3RD_LIBS += Third-party/TencentMcareV31/Arm_lib/TencentMcare.a
			else
			3RD_LIBS += Third-party/TencentMcareV31/Arm_lib/TencentMcareV31_No_Stack.a
			endif
		endif
endif
#==========================================================================================
#=======================================END================================================
#==========================================================================================