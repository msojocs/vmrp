分析并修复执行命令`test/gxdzc/audio-enable.sh`启动应用后，自动点击进入音乐设置界面，点击“确定”没反应。

`123,291,500`点击为触发“退出游戏”选项。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

注意：
在解决完BUG后，再去考虑兼容`build/vmrp mythroad/nes.mrp`
`build/vmrp mythroad/dota.mrp`
`build/vmrp mythroad/mrpinfo.mrp`

分析并修复执行命令`test/gghjt/pay-normal-back.sh`启动应用后，自动点击进入下载界面，并返回，但是返回没有反应。

预期结果：返回到游戏主菜单。
实际结果：实际还在下载界面。

最后一个`227,308,2000`点击为触发返回事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。


分析并修复执行命令`test/gghjt/pay-normal-enter.sh`启动应用后，自动点击进入付费界面，但是实际界面还在游戏主菜单界面。

预期结果：能看到付费界面。
实际结果：实际还在主菜单。

最后一个`121,291,3000`点击为触发进入付费界面事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gghjt/continue-twice.sh`启动应用后，自动点击进入下载界面，返回再进入，会卡在游戏主菜单,关不掉,只能pkill。

预期结果：第二次以及后续还能成功进入。
实际结果：只能进下载界面一次。

`121,291,3000`点击为触发进入付费界面事件。
`160,290`点击为切换菜单.

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/pay.sh`启动应用后，自动点击进入付费界面，但是实际界面卡在“请稍后...”。

预期结果：能看到付费界面。
实际结果：卡在“请稍后...”。

最后一个`133,295,3000`点击为触发进入付费界面事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/pay-cancel.sh`启动应用后，自动点击进入付费界面，但是取消付费会报错。

预期结果：不报错。
实际结果：会报错。


程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/download-resource-twice.sh`启动应用后，自动点击进入下载界面，返回再进入，会停留在游戏主菜单。

预期结果：第二次以及后续还能成功进入。
实际结果：只能进下载界面一次。
`temp/mythroadlib_disasm`可以参考，来分析模拟器的逻辑。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。


分析并修复执行命令`test/gghjt/download-plugin-enter.sh`启动应用后，自动点击进入下载界面，但是画面卡在菜单界面。

预期结果：显示下载界面。
实际结果：显示菜单界面。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}


分析并修复执行命令`test/gghjt/pay-normal-back.sh`启动应用后，自动点击进入付费插件下载界面，但是返回后，界面一直闪烁。

预期结果：返回后界面稳定显示游戏菜单。
实际结果：返回后，界面一直闪烁。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。