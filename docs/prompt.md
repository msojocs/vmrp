分析并修复执行命令`test/gxdzc/audio-enable.sh`启动应用后，自动点击进入音乐设置界面，点击“确定”没反应。

`123,291,500`点击为触发“退出游戏”选项。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

注意：
在解决完BUG后，再去考虑兼容`build/skyengine mythroad/nes.mrp`
`build/skyengine mythroad/dota.mrp`
`build/skyengine mythroad/mrpinfo.mrp`

分析并修复执行命令`test/gghjt/pay-normal-back.sh`启动应用后，自动点击进入下载界面，并返回，但是返回没有反应。

预期结果：返回到游戏主菜单。
实际结果：实际还在下载界面。

最后一个`227,308,2000`点击为触发返回事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。


分析并修复执行命令`test/gghjt/pay-normal-enter.sh`启动应用后，自动点击进入付费界面，但是实际界面还在游戏主菜单界面。

预期结果：能看到付费界面。
实际结果：实际还在主菜单。

最后一个`121,291,3000`点击为触发进入付费界面事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gghjt/continue-twice.sh`启动应用后，自动点击进入下载界面，返回再进入，会卡在游戏主菜单,关不掉,只能pkill。

预期结果：第二次以及后续还能成功进入。
实际结果：只能进下载界面一次。

`121,291,3000`点击为触发进入付费界面事件。
`160,290`点击为切换菜单.

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/pay.sh`启动应用后，自动点击进入付费界面，但是实际界面卡在“请稍后...”。

预期结果：能看到付费界面。
实际结果：卡在“请稍后...”。

最后一个`133,295,3000`点击为触发进入付费界面事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/pay-cancel.sh`启动应用后，自动点击进入付费界面，但是取消付费会报错。

预期结果：不报错。
实际结果：会报错。


程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/download-resource-twice.sh`启动应用后，自动点击进入下载界面，返回再进入，会停留在游戏主菜单。

预期结果：第二次以及后续还能成功进入。
实际结果：只能进下载界面一次。
`temp/mythroadlib_disasm`可以参考，来分析模拟器的逻辑。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。


分析并修复执行命令`test/gghjt/download-plugin-enter.sh`启动应用后，自动点击进入下载界面，但是画面卡在菜单界面。

预期结果：显示下载界面。
实际结果：显示菜单界面。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}


分析并修复执行命令`test/gghjt/continue-confirm.sh`启动应用后，自动点击开始下载插件，但是一直没有结果。

预期结果：显示成功或失败。
实际结果：日志一直在循环
[INFO]mr_recv(2)
my_recv(s:2, len:0): checkReadable=1
my_recv(s:2): recv=0, errno=11
[INFO]mr_recv(2)
my_recv(s:2, len:0): checkReadable=1
my_recv(s:2): recv=0, errno=11
[INFO]mr_recv(2)
my_recv(s:2, len:0): checkReadable=1
my_recv(s:2): recv=0, errno=11

保存requestbody到文件，用curl模拟请求，然后对比程序来分析请求是否有问题。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

分析并修复执行命令`test/gghjt/pay-normal-back.sh`启动应用后，自动点击进入付费插件下载界面，但是返回后，界面一直闪烁。

预期结果：返回后界面稳定显示游戏菜单。
实际结果：返回后，界面一直闪烁。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gghjt/download-end-confirm.sh`启动应用后，自动点击开始下载付费插件，下载完成后点击确定加载付费插件，但是没有显示付费插件界面。

预期结果：显示付费插件界面。
实际结果：没有显示付费插件界面，卡在“下载已完成”界面。

`5,308,2000`是点击“下载已完成”界面的“确定”按钮。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

分析并修复执行命令`test/dota/hp.sh`启动应用后，界面花屏的问题。

预期结果：正常的可视界面。
实际结果：花屏界面。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}


分析并修复执行命令`test/dota/hp.sh`启动应用后，界面有一块图像丢失的问题。

预期结果：正常的可视界面。
实际结果：有一块区域时黑色的界面。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

分析并修复执行测试用例`test/e2e/dota/download-plugin.test.ts`自动点击进入浏览器插件下载界面，返回，第二次进入没有反应。

预期结果：可以重复进入浏览器插件下载界面。
实际结果：只能进入一次，第二次无法进入。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

分析并修复执行命令`test/dota/community-enter.sh`，自动点击进入社区插件下载界面，但是界面主体文本描述没有显示。

预期结果：能看到下载描述文字。
实际结果：可以看到title，左下角按钮，右下角按钮；但是主体描述区域是黑的。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

分析并修复执行测试用例`pnpm vitest run test/e2e/gghjt/game-start.test.ts -t 不缺渲染`自动进入游戏界面，但是顶部和底部渲染缺失。

预期结果：可以完整显示界面。
实际结果：存在顶部和底部图像丢失。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。


分析并修复执行测试用例`pnpm vitest run test/e2e/optwar/game-prepare.test.ts`自动进入游戏界面，但是顶部广告条没有消失。

预期结果：广告条可以消失。
实际结果：点击左方向键或右方向键后广告条应该要消失，然后菜单能切换。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。


分析并修复执行测试用例`pnpm vitest run test/e2e/gwkdl/game-prepare.test.ts`自动进入游戏界面，但是logo以及游戏图像没有加载出来。

预期结果：logo和游戏图像可以显示。
实际结果：纯色背景，图像没有显示。
两张logo显示失败。
“请按任意键”界面加载图像`zm.bmp`失败。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。文件:`docs/gwkdl-image-debug.md`
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
这个游戏依赖扩展内存，可以注意一下这个信息。
充分利用子Agent。
记得存储记忆数据。


分析并修复执行测试用例`pnpm vitest run test/e2e`，`ce382f4488948934fcdaff4811289ac1377a9aec`commit是针对gwkdl的修复，但是这导致了其它测试用例失败。

预期结果：所有测试用例通过。
实际结果：部分用例失败。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。文件:`docs/gwkdl-image-debug.md`
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
这个游戏依赖扩展内存，可以注意一下这个信息。
充分利用子Agent。
记得存储记忆数据。

分析这个应用启动失败`build/skyengine temp/RX4.5.mrp`

注意trace日志量会特别大。
不要使用xvfb。
深入反汇编检测问题所在位置。
你可以检测PPM，来验证调整是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
充分利用子Agent。
记得存储记忆数据。

分析并修复测试用例`pnpm vitest run test/e2e/optwar/exit-plugin.test.ts -t "游戏退出时下载插件"`退出时会自动下载应用超市并重启，但是卡在“重启中”界面。

预期结果：成功启动应用超市。
实际结果：卡在“重启中”界面。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。

分析命令`build/skyengine build/mythroad/wbrw.mrp`启动时会向服务器发送/simpleDownload请求下载资源/插件，分析缺失的文件路径。

可能是类似于：
```
if (!exists('path/to/file'))
  // request simpleDownload
```

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
深入反汇编检测目标所在位置。

充分利用子Agent。
记得存储记忆数据。

命令`build/skyengine build/mythroad/talkcat.mrp`启动后，界面图像存在部分损坏，分析并修复它。

预期结果：游戏主界面显示正常。
实际结果：进入后存在部分花屏形状的横线。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。


分析根据`test/e2e/wbrw/input-text.test.ts`步骤进入输入状态，粘贴文字后，输入框还是空白的，修复这个BUG。

预期结果：Ctrl+V可以成功粘贴文字。
实际结果：输入框仍然为空。
日志：INFO: editGetText(): 'http://wap.baidu.com'

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。9

测试用例`pnpm vitest run test/e2e/talkcat/game-prepare.test.ts -t "循环"`执行后，会卡死，分析并修复它。

预期结果：重复触发`下载 - 取消`不会卡死。
实际结果：一定次数的触发`下载 - 取消`会卡死。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。

命令`build/skyengine build/mythroad/gtcm.mrp`执行后，一直黑屏，分析并修复它。

预期结果：启动游戏。
实际结果：黑屏，没有任何显示。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。


测试用例`pnpm vitest run test/e2e/gzwdzjs/game-start.test.ts -t 花屏`执行后，会部分花屏，分析并修复它。

预期结果：部分黑屏。
实际结果：会部分花屏。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。


测试用例`pnpm vitest run test/e2e/gzwdzjs/game-start.test.ts -t 教程`执行后，游戏卡死，分析并修复它。

预期结果：游戏正常运行。
实际结果：柚子卡住不动。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。

测试用例`pnpm vitest run test/e2e/gzwdzjs/play.test.ts`执行后，游戏卡住，分析并修复它。

预期结果：游戏正常运行，20秒内显示获得的新植物。
实际结果：游戏卡住不动。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，再去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。

测试用例`test/e2e/cookie/run-mrp.test.ts`执行后，启动应用后，结尾退出子应用后，应该返回文件管理界面，但实际没有。

预期结果：返回文件管理器，文件管理器会显示“正在打开”，进入的状态是启动前的位置。
实际结果：退出子应用后，没有进入文件管理界面。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，最后一定要去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。


测试用例`pnpm vitest run test/e2e/wbrw/proxy.test.ts`执行最后，会访问代理服务器来进行网页加载，目前不能下载文件。

预期结果：访问http://mrp.gddhy.net/mrp/sky_SaiNes-v1010/，点击`sky_SaiNes-v1010.mrp`，触发下载功能。
实际结果：目前点击下载目标实际是加载二进制网页，没有触发下载。

tool/server-http.js已启动，修改tool/proxy.js来实现代理服务器。

测试用例只是用来复现流程的，你需要调整逻辑符合预期功能，它的通过不代表成功，要检查截图。
真机参考数据：temp/brw/

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测代理机制。
一定要截图检测PPM，来验证实现是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}
没有修改C代码就不要执行E2E测试用例。

在分析与解决过程中，把进度定期保存至文档中。
充分利用子Agent。
记得存储记忆数据。


测试用例`pnpm vitest run test/e2e/wbrw/proxy.test.ts`执行特定操作会下载mrp文件，目前没有接口实现。

预期结果：访问`菜单-设置-设置中心-浏览设置-字体-16号字体`会提示下载字体。
实际结果：目前`/mrp`接口还没有实现。

tool/server-http.js已启动，修改tool/proxy.js来实现代理服务器。

测试用例只是用来复现流程的，你需要调整逻辑符合预期功能，它的通过不代表成功，要检查截图。
真机参考数据：temp/brw/
目标字体文件：temp/wfnt16c.mrp

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测代理机制。
一定要截图检测PPM，来验证实现是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}
没有修改C代码就不要执行E2E测试用例。

在分析与解决过程中，把进度定期保存至文档中。
充分利用子Agent。
记得存储记忆数据。

复现脚本`test/e2e/wbrw/temp.test.ts`执行会自动访问网页，但是页面没有换行，是否拥挤。
注意：不是测试用例，只是复现脚本，只需要保存网页加载后的图像，检测是否符合预期即可。

预期结果：`软件列表`下面显示一条水平线。
实际结果：没有换行

项目源码：`~/github/sky-engine-tools`，修改源码会热重载。

在分析与解决过程中，把进度定期保存至文档中。
充分利用子Agent。
记得存储记忆数据。

工作区中的修改是修复以下BUG产生的，分析实现方案是否合理：
命令`build/skyengine build/mythroad/愤怒的小鸟VS僵尸2_v1002-3.mrp`执行后，`build/mythroad/cache`会出现乱码文件夹，分析哪里来的，如果是程序释放资源的文件夹，那就修正编码。


程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，最后一定要去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。

工作区中的修改是修复以下BUG产生的，分析有必要改这么多代码吗
测试用例`pnpm vitest run test/e2e/gjxwsmn/boot-to-menu.test.ts`执行后，游戏界面渲染缺失。

预期结果：游戏界面渲染正常。
实际结果：渲染缺失。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写硬编码代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，最后一定要去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。

测试用例`pnpm vitest run test/e2e/gsha/download-res.test.ts`执行后，会下载资源包，但是没有进度，分析并修复。

预期结果：资源包开始下载。
实际结果：看起来没有发起网络请求。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写硬编码代码，如if(is_xxx_app()) {...}
禁止硬编码

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，最后一定要去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。


分析代码，想办法把`src/app_compat_gghjt.c`和`src/app_compat.c`的硬编码逻辑去掉，改成通用逻辑。
程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写硬编码代码，如if(is_xxx_app()) {...}

在分析与解决过程中，把进度定期保存至文档中。
先不管其它测试用例，先修好BUG，最后一定要去兼容其它测试用例。
充分利用子Agent。
记得存储记忆数据。
