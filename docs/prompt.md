分析并修复执行命令`test/gxdzc/audio-enable.sh`启动应用后，自动点击进入音乐设置界面，点击“确定”没反应。

`123,291,500`点击为触发“退出游戏”选项。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

注意：
在解决完BUG后，再去考虑兼容`build/vmrp mythroad/nes.mrp`
`build/vmrp mythroad/dota.mrp`
`build/vmrp mythroad/mrpinfo.mrp`

分析并修复执行命令`test/gghjt/pay-normal-back.sh`启动应用后，自动点击进入下载界面，并返回，但是返回没有反应。

预期结果：返回到游戏主菜单。
实际结果：实际还在下载界面。

最后一个`227,308,2000`点击为触发返回事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。


分析并修复执行命令`test/gghjt/pay-normal-enter.sh`启动应用后，自动点击进入付费界面，但是实际界面还在游戏主菜单界面。

预期结果：能看到付费界面。
实际结果：实际还在主菜单。

最后一个`121,291,3000`点击为触发进入付费界面事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gghjt/continue-twice.sh`启动应用后，自动点击进入下载界面，返回再进入，会卡在游戏主菜单,关不掉,只能pkill。

预期结果：第二次以及后续还能成功进入。
实际结果：只能进下载界面一次。

`121,291,3000`点击为触发进入付费界面事件。
`160,290`点击为切换菜单.

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/pay.sh`启动应用后，自动点击进入付费界面，但是实际界面卡在“请稍后...”。

预期结果：能看到付费界面。
实际结果：卡在“请稍后...”。

最后一个`133,295,3000`点击为触发进入付费界面事件。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/pay-cancel.sh`启动应用后，自动点击进入付费界面，但是取消付费会报错。

预期结果：不报错。
实际结果：会报错。


程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gxdzc/download-resource-twice.sh`启动应用后，自动点击进入下载界面，返回再进入，会停留在游戏主菜单。

预期结果：第二次以及后续还能成功进入。
实际结果：只能进下载界面一次。
`temp/mythroadlib_disasm`可以参考，来分析模拟器的逻辑。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。


分析并修复执行命令`test/gghjt/download-plugin-enter.sh`启动应用后，自动点击进入下载界面，但是画面卡在菜单界面。

预期结果：显示下载界面。
实际结果：显示菜单界面。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}


分析并修复执行命令`test/gghjt/continue-confirm.sh`启动应用后，自动点击开始下载插件，但是一直没有结果。

预期结果：显示成功或失败。
实际结果：日志一直在循环
[INFO]mr_recv(2)
my_recv(s:2, len:0): checkReadable=1
my_recv(s:2): recv=0, errno=11
[INFO]mr_recv(2)
my_recv(s:2, len:0): checkReadable=1
my_recv(s:2): recv=0, errno=11
[INFO]mr_recv(2)
my_recv(s:2, len:0): checkReadable=1
my_recv(s:2): recv=0, errno=11

保存requestbody到文件，用curl模拟请求，然后对比程序来分析请求是否有问题。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

分析并修复执行命令`test/gghjt/pay-normal-back.sh`启动应用后，自动点击进入付费插件下载界面，但是返回后，界面一直闪烁。

预期结果：返回后界面稳定显示游戏菜单。
实际结果：返回后，界面一直闪烁。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。

分析并修复执行命令`test/gghjt/download-end-confirm.sh`启动应用后，自动点击开始下载付费插件，下载完成后点击确定加载付费插件，但是没有显示付费插件界面。

预期结果：显示付费插件界面。
实际结果：没有显示付费插件界面，卡在“下载已完成”界面。

`5,308,2000`是点击“下载已完成”界面的“确定”按钮。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}

分析并修复执行命令`test/dota/hp.sh`启动应用后，界面花屏的问题。

预期结果：正常的可视界面。
实际结果：花屏界面。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}


分析并修复执行命令`test/dota/community-enter.sh`，自动点击进入社区插件下载界面，但是界面主体文本描述没有显示。

预期结果：能看到下载描述文字。
实际结果：可以看到title，左下角按钮，右下角按钮；但是主体描述区域是黑的。

程序的现有代码不一定正确。
注意trace日志量会特别大。
不要使用xvfb。
修改代码要加注释，不要有兜底代码逻辑。
深入反汇编检测BUG所在位置。
你可以检测PPM，来验证修复是否成功。
不能写特定场景代码，如if(is_xxx_app()) {...}