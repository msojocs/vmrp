---
title: "WBRW Go html2sky rendering progress 2026-07-12"
tags: ["wbrw", "html2sky", "sky-format", "ppm", "progress"]
created: 2026-07-12T10:37:19.291Z
updated: 2026-07-13T06:35:53Z
sources: []
links: []
category: session-log
confidence: medium
schemaVersion: 1
---

# WBRW Go html2sky rendering progress 2026-07-12

Goal: make /home/msojocs/github/mrp-store/backend/internal/html2sky/html2sky.go generate WBRW-renderable SKY for test/e2e/wbrw/temp.test.ts, covering logo text, input, select, button, and local light-green background, without app-specific branches. Current state: mrp-store backend has an untracked backend/internal/html2sky directory with substantial Go generator/tests; go test ./internal/html2sky passes, but this is not PPM proof. Evidence from vmrp wiki/docs says WBRW SKY requires 0F record-dispatch token, per-row 4E nodes, link pool layout href/caption/companion, and op 0x38 only for focusable links. Current runtime: air runs mrp-store backend on :8089; direct GET /mrp/proxy2.51mrp.com/page2 returns retcode=0 for invalid body as expected; machine port 80 is nginx and Host: proxy2.51mrp.com currently returns 502, which may block real temp.test.ts independent of html2sky. Next: compare Go output with known JS/docs structure, remove invalid/unsupported display-list layout if needed, then run PPM verification through the real E2E path.

## 2026-07-12 continuation: dedicated element audit

- Fresh no-xvfb E2E passed and retained `/tmp/vmrp-e2e-O0m7Px/screen.ppm`; it visibly renders text approximations for the logo and controls plus the light-green region. This proves the blank-page defect is fixed, but not yet that the converter follows the sample's dedicated element grammar.
- Direct parsing of `temp/html2sky/32918048.sky` found 161 display records. The sample logo uses `0x4e -> 0x38 -> 0x45` with pool entries `./`, `./logo.png`, and the alt caption. The first form uses `0x61` (form), `0x62` (input/submit), and `0x41` (select); current Go emission only uses `0x4e/0x38/0x54` and therefore renders bracketed text instead of native elements.
- The sample record header can carry two optional u16 element fields (flag bits `0x8000` and `0x4000`); the current Go helper accepts only the latter. Pool references for `0x45/0x61/0x62` are one-based, while the anchor `0x38` caption operand is raw and activates the preceding href slot.
- Proxy reverse engineering confirms the live path is WBRW `game.ext` -> nginx `:80` -> mrp-store `:8089` -> `html2sky.Generate`. The response gate at `game.ext+0x41c60` requires HTTP status 200 and tag33=3; the current online packet satisfies both. Image requests already have a generic Go `/image` fetch route.
- Next experiment: add generic image/form model data and dedicated records without URL/app checks or fallback emission, then rerun E2E and inspect both `/image` traffic and raw PPM pixels. Historical Node experiments reported an unfinished image load, so a dedicated image record is not accepted until current Go `/image` behavior is proven by the live run.

## 2026-07-12 final native-element verification

- `html2sky.go` now retains linked image `src/alt`, hidden controls, raw control values, every select option, form grouping, and submit captions. The record writer supports both optional u16 fields from the `0x8000/0x4000` flags.
- Generated display records follow the sample layouts: linked logo `0x4e -> 0x38 -> 0x45 -> 0x38`; form `0x61`; hidden/text/submit `0x62` with type bytes 8/0/7; select `0x41` with style 10 and the duplicated name tail. Form/input/button payload lengths and one-based pool operands were corrected byte-for-byte against `32918048.sky`.
- The shared content node now uses neutral style 3 instead of tinting the entire root. Visible rows and native forms produce bounded light-green bands. Three appended CSS style records with no display references were removed; the verified sample style/glyph prelude remains mandatory before section `0x0f`.
- The logo record triggers a post-page `/image` request and receives HTTP 200. Current WBRW renders its UTF-8 alt caption `网盘搜索引擎` in the logo region rather than PNG pixels; the logo remains visibly identified without any converter fallback branch.
- `temp.test.ts` now explicitly writes `loaded.ppm` and asserts a >30,000-pixel page transition plus exact green/input/select/button pixels, so a blank final page can no longer pass. A 200ms log-flush wait removes the existing paste/stderr race.
- Final no-xvfb run passed in 24.81s: `/tmp/vmrp-e2e-tqv2Vy/loaded.ppm` (240x320, 122 colors, SHA-256 `11bb0823ee54fc54fc048790cd8e7dc4c39e1a1fa3a9e20b53991587bb50321e`). Exact RGB565 green `[208,252,136]` occupies 34,204 pixels with bbox `[5,57,230,295]`. Manual PNG inspection shows logo caption, native text field, select arrow, `搜索` and `跳页` buttons, and local light-green bands.
- Final static verification: `go test -count=1 ./internal/html2sky ./internal/service ./internal/handler` and `go vet` for the same packages pass. Production `html2sky.go` has no target host/path/app/fixture checks and no fallback/includeStylePrelude branch.

Post-audit update: removed the remaining `.c1/.title/.cpy/.c3` selector branches from production CSS handling, so no page-specific class names select rendering behavior. The fixed prelude and numbered element styles remain protocol data inferred from the SKY sample. Re-ran all Go tests/vet and the PPM-asserting E2E after this last edit. Authoritative final artifact is `/tmp/vmrp-e2e-sh49lG/loaded.ppm`, SHA-256 `8f21bebd1fef5203c5a2883f22e1d82ad9625ce51bc0d2cc93473e4a924f5233`; 240x320, 122 colors, 34,204 exact `[208,252,136]` pixels in bbox `[5,57,230,295]`. Image-path disassembly established that `game.ext+0x22760` draws alt when decoded width/height are non-positive; the 5291-byte PNG reaches the client byte-identically, so the current alt presentation is an emulator decode/completion issue rather than an HTML-to-SKY record defect.

## 2026-07-12 ordering correction

- User reference requires DOM order: inline/wrapped net-disk navigation first, then the native input/select/search form, then result rows. Removed `prioritizeInteractiveItems`, which had deliberately moved all controls before navigation and violated the source order.
- Added generic paragraph `FlowID` grouping. Links, active plain text, and retained `|` separators within one `<p>` share one `0x4e` row, so sibling `0x38/0x54` nodes flow inline and wrap naturally. Forms and result links continue in source order after that row. No class, URL, host, or app-specific branch was introduced.
- Added unit coverage for `navigation A/B < input < Result` and for exactly one `0x4e` row shared by an inline flow. Related Go tests and vet pass.
- The live remote URL currently returns zero `view.php` result links for every tested search source; it is therefore valid for its default PPM to transition from the search form directly to pagination. Default E2E still passed after the ordering change: `/tmp/vmrp-e2e-YWbvWe/loaded.ppm`, 240x320, 122 colors, SHA-256 `b42968c115e9b50ed7805bade1c1fedbd034d6d74878cd3bf300f285ee3dd2df`.
- To prove the complete reference order without fabricating production data, `temp.test.ts` accepts optional `VMRP_WBRW_TEST_URL`. A temporary local HTML page containing two genuine result DOM nodes was fetched through the same Go proxy/converter and rendered by WBRW. E2E passed and `/tmp/vmrp-e2e-9QAIjR/loaded.ppm` visibly shows net disks, input/select/搜索, then `qq2009.jar` and its description; SHA-256 `11fc5ae9280348fa11d8bcb34c3d6e498f976f42a7a15ed61b1a07574ce170a3`.

## 2026-07-12 text-background correction

- Background ownership is now inferred generically from matching tag, class, and id CSS selectors plus inline styles. Descendants inherit the nearest recognized container element; section green maps to SKY element `7`, the footer maps to `15`, and other explicit backgrounds map to neutral element `4`. No fixture class, host, path, or application name is encoded in production logic.
- Text in every recognized styled container now inherits the row background: linked text uses element `9`, while active text and separators use element `4`. Neutral result rows retain the body background instead of being incorrectly painted green, so navigation labels, result text, and footer text have no separate opaque white rectangle.
- Added a binary-record regression test that requires generated `0x54` link/plain records inside section and footer flows to use inheriting elements `[9, 4, 4]`. `go test -count=1 ./internal/html2sky ./internal/service ./internal/handler` and `go vet` for the same packages pass.
- Controlled end-to-end rendering through the real Go proxy/converter passed on the final build. `/tmp/vmrp-e2e-Ei9fpf/loaded.ppm` visibly shows navigation, native input/select/search, and two result rows in that order with inherited text backgrounds; SHA-256 `7974eca9b028280f2498f898fe653ec16c4da7dc7ca2fc1a4a11b9c5f2b8951c`.
- Default remote end-to-end rendering also passed on the final build. `/tmp/vmrp-e2e-WBJJR2/loaded.ppm` preserves the green navigation/form/pagination bands without white label boxes. Footer glyph gaps and surrounding pixels exactly match its `[248,252,176]` row background; SHA-256 `daffce499bee82456525f768c1df0c91f9d7c88b5d3c5926de130fd4d294aef8`.

## 2026-07-12 footer foreground correction

- The source CSS declares the footer foreground as `#aaa`, but generated footer text remained black. Parsing the reference `32918048.sky` established the exact `0x54` payload order as `operand, 0x00, red, green, blue`; the generator previously emitted the control byte after RGB.
- Corrected all generated `0x54` link/plain payloads to the reference byte order. Footer-palette CSS rules now populate `MutedTextColor`, and plain text under container element `15` uses it. The implementation remains selector-agnostic.
- Unit coverage now checks fixture extraction of `#aaa`, generated link/body/footer colors, and the inheriting text elements. Go tests and vet pass.
- The E2E test now asserts a real footer glyph pixel. Final remote run passed with CSS `#aaa` rendered as framebuffer RGB `[168,168,160]` over `[248,252,176]`; `/tmp/vmrp-e2e-YP7sEa/loaded.ppm`, SHA-256 `4b72deffbe5050f0c49ac9f64071f57303338d3c3e8d9de7e836f6fe2e59ee5a`.

## 2026-07-13 image decoding continuation

- The active success criterion is stricter than the previous page-rendering audit: the bitmap logo itself must be visible. Rendering the image `alt` caption is failure for this continuation.
- Current Go `/image` behavior fetches the upstream bytes and returns `makePacket(image, nil)` with proxy packet status 200. Its unit test proves byte preservation only; it does not prove that WBRW publishes decoded dimensions or draws bitmap pixels.
- Previous runtime evidence says the upstream 5291-byte PNG reached the client byte-identically, while `game.ext+0x22760` selected `alt` because decoded width or height remained non-positive. This focuses the new investigation on the exact `/image` response metadata/type contract and the asynchronous decoder completion path, not merely upstream reachability.
- Required final evidence: no-xvfb execution of `test/e2e/wbrw/temp.test.ts`, a retained `loaded.ppm`, and pixel/visual comparison proving that the bitmap logo region contains the source image rather than only its caption.
- Fresh pre-fix baseline passed its existing page assertions in 26.86s but still rendered only the `网盘搜索引擎` alt caption. Evidence: `/tmp/vmrp-e2e-dsSJ8F/loaded.ppm`, SHA-256 `8ea39b87ffe1a989d83fe9724425fa989482687874f88698e7854092033eaf5d`; the workspace stored the byte-identical 5291-byte, 273x54 PNG as `cache3/6421.dat`, while `index.dat` remained a 16-byte empty header.
- Reverse engineering closed the cache classification path. `game.ext+0x1c870` requests response field 7; when absent, `+0x1c890` stores object type 0. `+0x1c91a/+0x1c926` pass that type to `+0x10cf8`, whose `+0x10d12` comparison maps type 100 to `.gif` at `+0x10d16`; unknown/type 0 selects `.dat` at `+0x10d4e`. The field is syntactically optional to the packet parser but functionally required for image cache registration.
- The first `tag7=100` experiment correctly changed the cache suffix from `.dat` to `.gif`, but PPM still showed alt. Further disassembly proved 100 is GIF specifically, not a generic image class: `game.ext+0x10cf8` maps 100..105 to `.gif/.jpg/.png/.tiff/.bmp/.slg`. The `.gif` reader consumed PNG signature bytes 6..9 as little-endian GIF dimensions and obtained height 0.
- Cache admission also needs real freshness metadata. `tag20` is the Unix timestamp parsed from upstream `Last-Modified`, not payload length; the raw HTTP path at `+0x1cafa` proves the same field ownership. The target value is `0x54fb4fa4` (`1425756068`), matching `Sat, 07 Mar 2015 19:21:08 GMT`. A temporary length-valued experiment was rejected and replaced before final verification.
- The Go response now classifies the actual encoded bytes: GIF=100, JPEG=101, PNG=102, TIFF=103, BMP=104. Unsupported/non-image bytes produce an explicit proxy2 415 envelope. Payload bytes remain unchanged, and a valid upstream `Last-Modified` is forwarded as `tag20`; there is no URL, host, app, or fixture branch and no guessed format/default timestamp.

## 2026-07-13 final bitmap-logo fix

- The preceding raw-payload statement is superseded by the final compatibility result below. Correct cache classification alone publishes a valid `.png`, but this WBRW platform cannot decode it: the private image dispatcher at `game.ext+0x46070` returns `-1` for selector `12`, which is the PNG/JPEG open path used by `+0x22dde..0x22e1e`.
- Narrow no-xvfb traces proved the server response contract was already complete. The image cache record was inserted through `+0x1c970 -> +0xfe18 -> +0x1008c`, EOF reached `+0x1de46`, and browser completion reached `+0x1dc18 -> +0x20ce4`. No additional response TLV is valid: tag 21 enables framed decompression, nonzero tag 33 and tags 29/30 bypass normal cache publication, and tag 8 only overrides envelope payload length.
- WBRW's in-process image completion path directly recognizes only `.bmp` and `.gif` at `+0x22cae..0x22cd8`. The Go proxy now performs a generic transport adaptation: valid GIF/BMP streams pass through unchanged after complete decode validation; decodable PNG/JPEG are converted with Go's standard image codecs to GIF; TIFF and malformed/unknown inputs return an explicit proxy2 415 error. The emitted tag 7 always matches the actual output bytes. There is no application, URL, host, or fixture branch and no format fallback.
- The target upstream remains a 5291-byte, 273x54 RGBA PNG with SHA-256 `1f57029ad31e0f5cd9815f96a9f28c46c82af9573eb37d24242b0e6ad69e3f78`. WBRW ignores standards-valid GIF transparency and originally exposed the transparent index's black RGB. The converter now derives a matte from the dominant minimum-alpha source color and composites alpha before quantization. The final cache artifact is `/tmp/vmrp-ws-B6b9e6/mythroad/brw/http/cache3/5898.gif`, an opaque 273x54 GIF89a with white logical background and SHA-256 `a5a2cc0f0896adb619c451d69cde206be3422e22d0bc3371d8eb25c30e0630e8`.
- `temp.test.ts` now proves bitmap rendering rather than alt text. It requires a blue cloud pixel family at `(50,43)` and the exact red ellipse pixel `[248,0,0]` at `(50,82)`, then checks the shifted green section and native form surfaces. The final PPM has 151 colors; blue-logo pixels count 1118 in bbox `[8,41,93,91]`, red pixels count 77 in bbox `[43,78,57,84]`, and exact green `[208,252,136]` occupies 26717 pixels in bbox `[5,100,230,275]`.
- The image handler now requires exact-length, code-2, zero-payload envelopes with nonempty path/host, no duplicate target fields, no unexplained header/body bytes, and only ASCII plus trailing NUL terminators. Invalid requests return proxy2 400. Dedicated tests construct WBRW's request code independently of the configured response packet code.
- Unit coverage validates complete GIF/BMP pass-through and rejects truncated GIF/JPEG/PNG/BMP, TIFF, and unknown input instead of accepting recognizable signatures as valid images.
- A completion audit proved WBRW publishes only when unsigned tag 20 is nonzero or signed tag 15 is positive. The handler derives tag 15 from `max-age`, then `Expires`, then Last-Modified heuristic freshness and subtracts corrected current age (`max(now-Date, Age+response-delay+resident-time)`). It returns proxy2 502 instead of a false 200 when no publishable metadata exists. Quote-aware directive parsing handles field-qualified `no-cache`; `no-store` and invalid/unrepresentable metadata fail explicitly.
- Image fetches request only GIF/PNG/JPEG/BMP with identity encoding and cap encoded bodies at 4 MiB. Still images are capped at 4,194,304 pixels; GIF structure is scanned before allocation to cap both 256 frames and 4,194,304 aggregate frame pixels. Resource-limit oversize input returns proxy2 413; no default format or TTL fallback exists.
- Final no-xvfb E2E with a direct non-black background assertion passed in 24.72s. Artifact: `/tmp/vmrp-e2e-XLLZsd/loaded.ppm`, 240x320, 150 colors, SHA-256 `1239a429c4d1689ce0b3c55d0daf64b32ae62f40b6b864cd2067554dab1c993e`. Manual inspection of `/tmp/vmrp-e2e-XLLZsd/loaded.png` shows the logo on framebuffer white `[248,252,248]`, with no black canvas, above the navigation/form content. Focused Go tests, vet, race, module tidy diff, and repository diff checks pass.

## 2026-07-13 image viewport fitting

- The working opaque `273x54` bitmap still extended past WBRW's right content edge. Retained proxy2 evidence distinguishes viewport tag 4=`236x266`, explicit image-size tag 5 (for example `35x35`), and image layout bound tag 31=`220x0` or `236x0`. The current live logo request supplies `220x0`.
- The image request parser now validates tag 31 and rejects duplicate, wrong-length, and all-zero bounds. The adapter only scales down and preserves aspect ratio with Catmull-Rom resampling. It does not use a URL, host, application, fixture, or hard-coded screen width. Unconstrained and already-fitting images are not enlarged.
- PNG/JPEG continue through the opaque GIF compatibility path. Static GIF/BMP are resized only when required and otherwise remain byte-identical; a multi-frame GIF requiring resize fails explicitly instead of losing frames. Unit coverage proves `273x54 -> 220x44`, a height-limited case, no upscaling, GIF/BMP format retention, and handler-level tag 31 behavior.
- Final no-xvfb E2E passed in 24.73s. `/tmp/vmrp-e2e-nQzH6Y/loaded.ppm` is 240x320 with 156 colors, SHA-256 `77b7e55d4af75b13f8f506b6a654e9f261781bf2beb15fff149e151b371394e5`. Visual inspection of `/tmp/vmrp-e2e-nQzH6Y/loaded.png` shows the complete rightmost logo glyph; the regression asserts its pixel at `(216,53)` along with logo colors, matte, controls, and footer.
- `/tmp/vmrp-ws-0UsxRf/mythroad/brw/http/cache3/6131.gif` is an opaque, white-background, single-frame GIF89a at `220x44`, SHA-256 `0d5c2e3e36801a82b31612cd2a5e2f49486a4eeee1bba281d15464d73048a3c8`. Focused tests/vet/race pass and module tidy is unchanged. Full tests retain only the known missing html2sky fixture failures; full vet additionally reports the pre-existing Go 1.23 versus `testing.Chdir` version mismatch.

## 2026-07-13 continuous CSS container backgrounds

- The live HTML places the pagination paragraph and jump form paragraph under one `<div class="c1">` whose matching CSS rule supplies `background-color:#d2ff8d`. Extraction already propagated the color but discarded the shared DOM-container identity, so the generator emitted two independent green rows and their margins exposed the white body background.
- The reference `32918048.sky` proves the required hierarchy without relying on the class name: bounded `element 7` node 138 owns paragraph rows 139/146 with `element 8` and form node 147 with `element 9`. The Go model now assigns a generic `ContainerID` whenever any supported tag/class/id selector or inline style provides a background. Descendant flows and forms retain that ID; separate matching DOM nodes receive distinct IDs.
- SKY generation emits one bounded parent per `ContainerID`, nests paragraph rows and forms under it, and validates that one container never changes element style. A form nested in a paragraph retains the reference empty paragraph row before its sibling `0x61` form node. There is no `.c1`, URL, host, text, or application-specific production branch.
- Unit regressions verify shared versus distinct container IDs and parse the generated records to require one `element 7` parent with two `element 8` rows and one `element 9` form. Focused html2sky/service/handler tests, focused vet, and race tests pass.
- Final no-xvfb E2E passed in 24.64s. `/tmp/vmrp-e2e-OWLU99/loaded.ppm` is 240x320 with 156 colors, SHA-256 `81c00f673598d282c609b6b186abbfe5d809bbb9b275bd727a600c5df3450eaf`. Manual inspection of `/tmp/vmrp-e2e-OWLU99/loaded.png` shows the pagination and jump rows on one continuous green background. E2E checks both `x=10` and `x=220` for every `y=228..242`, then checks every pixel across `x=5..230` at `y=234`; all 256 samples are exact framebuffer green `[208,252,136]`. Exact green occupies 32,394 pixels with bbox `[0,80,230,262]`.
- Full `go test ./...` remains red only for the same three tests that require the absent `temp/html2sky/网盘搜索引擎.html` fixture; all other packages pass.
