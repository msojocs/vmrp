---
title: "WBRW proxy2 font MRP download"
tags: ["wbrw", "proxy2", "font", "mrp", "download", "reverse-engineering", "e2e", "sd", "http-range", "install", "arm-ext"]
created: 2026-07-11T08:57:36.289Z
updated: 2026-07-11T11:28:02.844Z
sources: []
links: ["wbrw-proxy2-web-page-rendering.md"]
category: debugging
confidence: medium
schemaVersion: 1
---

# WBRW proxy2 font MRP download

2026-07-11 fixed the WBRW settings path Menu > Settings > Settings Center > Browse Settings > Font > 16-font download. Direct evidence: game.ext+0x21640 maps proxy2 types 0..4 to /page2,/image,/mrp,/res,/sta; game.ext+0x17AB8 selects /mrp for dmrp.wapproxy.sky-mobi.com and /res for other download hosts; both types share tag10 continuation building at +0x21474. The ordinary download response path at +0x17488 requires tag7 at +0x175B4 and parses tag16 ranges. Existing tool/temp/_mrp_1783742320649.bin and five retries proved /mrp was emitted but tool/proxy.js fell through to helloworld. The generic fix indexes PROXY2_MRP_ROOT (default temp) by query appid against MRP header LE appid at offset 68, rejects missing/duplicate/malformed resources explicitly, pins the first validated package bytes for the process lifetime, and sends 1800-byte proxy2 chunks with tag7=u16 22, tag8=total-offset, tag16=>III(start,end,total). No app name, menu, host-specific application branch, or appid constant is used. temp/wfnt16c.mrp is 22302 bytes, appid 422006, SHA-256 98763f1c6bff5cdef6a3fd3c22e34168d0445359f9fdcb3c34bc381e85dbac3a. Final E2E passed; retained evidence /tmp/vmrp-e2e-Y4X8x9 and /tmp/vmrp-ws-JIB10I. Actual /mrp offsets were 0,1800,...,21600; final installed mythroad/brw/wfnt16c.mrp matched the fixture byte-for-byte. PPMs show the 16-font selection, the explicit no-font/download prompt, download manager at 1K/21K, and download-success/install prompt. temp/brw was absent, so no true-device packet/PPM comparison was possible. Related prior work: [[wbrw-proxy2-web-page-rendering]].

---

## Update (2026-07-11T09:13:36.888Z)

2026-07-11 fixed the WBRW settings path Menu > Settings > Settings Center > Browse Settings > Font > 16-font download. Direct evidence: game.ext+0x21640 maps proxy2 types 0..4 to /page2,/image,/mrp,/res,/sta; game.ext+0x17AB8 selects /mrp for dmrp.wapproxy.sky-mobi.com and /res for other download hosts; both types share tag10 continuation building at +0x21474. The ordinary download response path at +0x17488 requires tag7 at +0x175B4 and parses tag16 ranges. The generic fix validates code=3, the configured dmrp host, /sd, and type=2; errors retain tag7 and never fall back. The first request reads the complete MRP through one header-validated fd, rechecks fstat/header, then stores a bounded immutable appid snapshot in process-global state. All continuations and concurrent clients slice the same bytes, so path replacement cannot cause TOCTOU or cross-version assembly; fixture changes require a server restart. No app-name/menu/appid-specific branch exists. Final evidence is /tmp/vmrp-e2e-Y4X8x9 and /tmp/vmrp-ws-JIB10I: one initial /mrp plus tag10 offsets 1800..21600, exact 22302-byte installed package, converged download metadata, no .dt, and prompt/progress/success PPMs. Actual DISPLAY=:0 was used without xvfb or large trace. temp/brw was absent, so no true-device comparison was possible. Related prior work: [[wbrw-proxy2-web-page-rendering]].

---

## Update (2026-07-11T09:36:24.799Z)

Direct HTTP /sd was added as a second entry point to the same bounded process-global immutable MRP snapshot used by proxy2 /mrp. The route claims only the configured dmrp host and exact /sd path, requires one type=2 and one decimal uint32 appid, resolves only a unique top-level MRP whose header appid at offset 68 matches, and never falls back by filename or scenario. GET supports full 200 and one closed, open-ended, or suffix Range with 206; HEAD returns full metadata; a SHA-256 strong ETag pins If-Range, and mismatches return the full 200 representation. Parameter, resource, method, and range failures return explicit 400, 404, 405, or 416 responses. /sd dispatches before request-body collection. Hot reload upgrades older in-memory snapshots by calculating the missing ETag without changing their bytes. Verification: /sd full was 22302 bytes; ranges 0-1799, 1800-3599, and 21600-22301 matched the fixture; independently generated proxy2 /mrp requests produced 13 payloads at offsets 0,1800,...,21600 with tag7=22 and correct tag8/tag16. The rebuilt /mrp body, /sd body, fixture, and installed font all had SHA-256 98763f1c6bff5cdef6a3fd3c22e34168d0445359f9fdcb3c34bc381e85dbac3a. Retained final evidence: /tmp/vmrp-e2e-ikTR0a and /tmp/vmrp-ws-Ezi2zo. Final E2E passed in 23.91s; dllist1 contains the filename, MIME, full /sd URL, and equal completed/total lengths, dwnlist.dat is exact 010d0101, no .dt remains. The final test runs the browser proxy2 path before direct HTTP checks, asserts exactly 13 client offsets, and also resolves a second valid appid. The four PPMs visibly confirm 16-font focus, the missing-font download prompt, wfnt16c.mrp at 1K/21K, and the successful-download install prompt. The server now binds 127.0.0.1 by default; external exposure requires an explicit HOST override because Host is routing metadata, not authorization. The reverse-engineering boundary remains important: the current font client transports the /sd URL inside proxy2 POST /mrp; direct raw HTTP /sd is a service interface and shared-resource integration point, not a claim that the current font path performs a naked GET.

---

## Update (2026-07-11T11:28:02.844Z)

Final installation fix (2026-07-11): deep brwgraphics.ext disassembly proves +0x24C4 writes brw/wfnt16c.mrp into its private module record table[100], then +0x2040 invokes that same record table[125] to read 1008.uni. +0x7D0 returns failure only when this read fails, so the UI installation failure was not destination open/write. Host aex_t125 incorrectly read package/RAM slots 100/104/105 only from master EXT_TABLE_ADDR, losing the LR-owned nested ABI context and searching outer wbrw.mrp. The generic fix resolves arm_ext_resource_owner_for_lr, reads the private record pointer from owner->file_addr[0], validates one contiguous 106-slot span, and reads all package slots there. A nested invalid record fails explicitly without outer-package fallback; primary calls with no owner use the master table. Existing explicit non-root package precedence then reads brw/wfnt16c.mrp. Unit suite: 104 checks passed. The E2E uses the service on guest endpoint 127.0.0.1:80 and preserves Host proxy2.51mrp.com; no endpoint-remapping option is used. Exact test passed in 41.08s without xvfb or large trace. It observed exactly 13 /mrp offsets 0,1800,...,21600; wfnt16c.mrp was 22302 bytes/SHA-256 98763f1c6bff5cdef6a3fd3c22e34168d0445359f9fdcb3c34bc381e85dbac3a; installed mythroad/system/1008.uni was 34272 bytes/SHA-256 5e3515001810f6e25363a9152b4d449877aa24b38c17a9358b178af94c36e5e6. Retained PPM evidence: /tmp/vmrp-e2e-sqyAuE and /tmp/vmrp-e2e-NTbM4h; workspace /tmp/vmrp-ws-c4Dp1J. Visual inspection confirms download prompt, 1K/21K progress, download-success/install prompt, no install-failure dialog, and after same-workspace restart the 16-font radio marker plus settings value Font: 16-font. Restart generated no new /mrp requests.
